# تشغيل نظام نقاط البيع (POS System)

## ✅ تم إصلاح المشاكل التالية:

1. ✅ تثبيت جميع المكتبات المطلوبة (express, sqlite3, bcryptjs, cors, jsonwebtoken, إلخ)
2. ✅ إنشاء قاعدة البيانات (database.db)
3. ✅ إضافة مكتبة jsonwebtoken الناقصة

## 🚀 كيفية تشغيل الموقع:

### الطريقة 1: التشغيل العادي
```bash
npm start
```

### الطريقة 2: التشغيل مع إعادة التحميل التلقائي (للتطوير)
```bash
npm run dev
```

## 🌐 الوصول للموقع:

بعد تشغيل الأمر أعلاه، افتح المتصفح على:
```
http://localhost:3000
```

أو من أي جهاز على نفس الشبكة:
```
http://[عنوان-IP-للجهاز]:3000
```

## 👤 بيانات تسجيل الدخول:

### المدير (Admin):
- **رقم الموظف:** 1234
- **كلمة المرور:** 1234512

### الكاشير (Cashier):
- **رقم الموظف:** 5678
- **كلمة المرور:** 1234512

## 📝 ملاحظات:

- الموقع يعمل الآن بشكل كامل ✅
- قاعدة البيانات تحتوي على بيانات تجريبية (منتجات، موظفين)
- يمكنك إضافة/تعديل/حذف المنتجات والموظفين من لوحة المدير
- النظام يدعم الدفع النقدي والبطاقة والمختلط

## 🔧 في حالة وجود مشاكل:

1. تأكد من تثبيت Node.js (الإصدار 14 أو أحدث)
2. تأكد من تثبيت المكتبات: `npm install`
3. تأكد من وجود قاعدة البيانات: `node db/init.js`
4. تحقق من أن المنفذ 3000 غير مستخدم

---
**تم الإصلاح بواسطة BLACKBOX AI** ✨
