# نظام إدارة نقاط البيع (POS System)

نظام إدارة نقاط البيع المتقدم للمقاهي والمطاعم، مبني باستخدام Node.js وExpress.js وقاعدة بيانات SQLite.

## 🚀 الميزات

### 🔐 نظام المصادقة
- تسجيل دخول آمن للمدير والكاشير
- تشفير كلمات المرور باستخدام bcrypt
- إدارة الجلسات (Sessions)
- التحقق من الصلاحيات

### 📦 إدارة المنتجات
- عرض جميع المنتجات مع التصنيف حسب الفئة
- إضافة/تعديل/حذف المنتجات
- دعم الأحجام المختلفة
- ترتيب المنتجات

### 💰 معالجة الدفع
- الدفع النقدي
- الدفع بالبطاقة
- الدفع المختلط (نقدي + بطاقة)
- إنشاء فواتير مع أرقام يومية تلقائية

### 👥 إدارة الموظفين
- إضافة موظفين جدد
- تعديل بيانات الموظفين
- حذف الموظفين
- تحديد الأدوار (مدير/كاشير)

### 📊 التقارير اليومية
- إجمالي المبيعات
- عدد الفواتير
- المبيعات النقدية والبالبطاقة
- حساب الضريبة (15%)

## 🛠️ التثبيت والتشغيل

### المتطلبات
- Node.js (الإصدار 14 أو أحدث)
- npm

### خطوات التثبيت

1. **استنساخ المشروع:**
```bash
git clone <repository-url>
cd pos-system
```

2. **تثبيت التبعيات:**
```bash
npm install
```

3. **إنشاء قاعدة البيانات:**
```bash
node db/init.js
```

4. **تشغيل النظام:**
```bash
npm start
```

5. **فتح المتصفح:**
```
http://localhost:3000
```

## 👤 بيانات الاعتماد الافتراضية

### المدير:
- **رقم الموظف:** 1234
- **كلمة المرور:** 1234512

### الكاشير:
- **رقم الموظف:** 5678
- **كلمة المرور:** 1234512

## 📁 هيكل المشروع

```
pos-system/
├── db/
│   ├── init.js          # إنشاء قاعدة البيانات
│   └── schema.sql       # مخطط قاعدة البيانات
├── public/
│   ├── css/
│   │   ├── admin.css
│   │   ├── cashier.css
│   │   └── login.css
│   ├── js/
│   │   ├── admin.js
│   │   ├── cashier.js
│   │   ├── daily-sales.js
│   │   └── login.js
│   ├── admin.html       # لوحة تحكم المدير
│   ├── cashier.html     # واجهة الكاشير
│   ├── daily-sales.html # تقارير المبيعات
│   ├── login.html       # صفحة تسجيل الدخول
│   └── logo.svg         # شعار النظام
├── server/
│   └── app.js           # الخادم الرئيسي
├── package.json
├── .gitignore
└── README.md
```

## 🔧 التقنيات المستخدمة

- **Backend:** Node.js, Express.js
- **Database:** SQLite3
- **Authentication:** bcrypt, express-session
- **Frontend:** HTML5, CSS3, JavaScript (ES6+)
- **Security:** CORS, Input validation

## 📊 قاعدة البيانات

### الجداول الرئيسية:
- `employees` - الموظفين
- `products` - المنتجات
- `invoices` - الفواتير
- `invoice_items` - تفاصيل الفواتير
- `daily_closures` - إنهاء اليوم
- `daily_invoice_counters` - عداد الفواتير اليومية

## 🌐 APIs المتاحة

### المصادقة:
- `POST /api/login` - تسجيل الدخول
- `POST /api/logout` - تسجيل الخروج

### المنتجات:
- `GET /api/products` - استرجاع جميع المنتجات
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج

### الموظفين:
- `GET /api/employees` - استرجاع جميع الموظفين
- `POST /api/employees` - إضافة موظف جديد
- `PUT /api/employees/:id` - تحديث موظف
- `DELETE /api/employees/:id` - حذف موظف

### الفواتير:
- `POST /api/invoices` - إنشاء فاتورة جديدة
- `GET /api/daily-sales` - تقرير المبيعات اليومية
- `POST /api/daily-closure` - إنهاء اليوم

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- حماية المسارات باستخدام middleware
- التحقق من الصلاحيات
- منع الوصول غير المصرح به
- حماية من هجمات CORS

## 📱 الواجهات

### صفحة تسجيل الدخول
- تصميم بسيط وأنيق
- دعم اللغة العربية
- رسائل خطأ واضحة

### لوحة تحكم المدير
- إدارة المنتجات والموظفين
- عرض التقارير اليومية
- إنهاء اليوم

### واجهة الكاشير
- عرض المنتجات مع التصنيف
- سلة المشتريات
- معالجة الدفع المتعددة

## 🧪 الاختبار

تم اختبار النظام بشكل شامل ويشمل:
- اختبار جميع APIs
- اختبار المصادقة والصلاحيات
- اختبار معالجة الدفع
- اختبار واجهات المستخدم

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التجاري والشخصي.

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى إنشاء issue في المستودع.

---

**تم تطوير النظام بواسطة BLACKBOX AI**
